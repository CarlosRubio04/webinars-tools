import styled from 'styled-components'

export const User = styled.div`
    text-align: center;
    position: relative;
    color: #fff;
    width: 500px;
    max-width: 100%;

    h2 {
        font-weight: 200;
        font-size: 28px;

        small {
            font-size: 18px;
            font-weight: 600;
        }
    }
    p {
        font-size: 14px;
    }
`
