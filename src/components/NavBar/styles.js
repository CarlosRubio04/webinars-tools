import styled from 'styled-components'

export const Nav = styled.nav`
    position: fixed;
    z-index: 99;
    top: 0;
    max-width: 100%;
    display: flex;

    & img {
        width: 30px;
    }

    .Nav-Container {
        padding: 10px 20px;
    }
`
